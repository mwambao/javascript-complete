//And operator represented by && operator.

let x = 1;
let y = 2;
let z = 3;

console.log(x < y && y < z); //true. This is because both conditions are satisfied.

console.log(x > y && y < z); //false. Because one part of the expression is not true.